export const PageBasic: React.FC = () => {
  // const navigator = useNavigate();
  // const [searchParams] = useSearchParams();
  // 登录成功后跳转回去
  // searchParams.get("continue") || "/", { replace: true }
  return <div>PageBasic</div>;
};
export default PageBasic;
